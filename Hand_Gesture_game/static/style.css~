/* A Witcher's Story - CSS Styles */
.option-button.disabled {
    opacity: 0.6;
    cursor: not-allowed;
    pointer-events: none;
}
@import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600&family=Crimson+Text:ital,wght@0,400;0,600;1,400&display=swap');

/* CSS Variables */
:root {
    --primary-gold: #b6b943;
    --dark-bg: #1a1a1a;
    --card-bg: rgba(40, 40, 40, 0.95);
    --text-light: #e0e0e0;
    --text-dim: #b0b0b0;
    --accent-red: #8b0000;
    --accent-blue: #2c5aa0;
}

/* Reset and Base Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Cinzel', regular;
    background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 50%, #0f0f0f 100%);
    min-height: 100vh;
    color: var(--text-light);
    position: relative;
    overflow-x: hidden;
}

/* Ensure full page layout */
html, body {
  height: 100%;
  margin: 0;
  padding: 0;
  position: relative;
}

/* Background container holds the image */
#backgroundContainer {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: -100; /* Stay behind everything */
  overflow: hidden;
}

/* Image covers the whole screen */
#backgroundImage {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
  filter: brightness(0.6); /* Optional: darken background */
  pointer-events: none;    /* Allow clicks through the image */
}

.gesture-guide {
  background: rgba(0, 0, 0, 0.5);
  border: 1px solid #b6b943;
  border-radius: 12px;
  padding: 10px 15px;
  font-family:  'Cinzel';
  color: #e4dcac;
  font-size: 14px;
  line-height: 1.5;
}

.gesture-guide h3 {
  color: #d1d35f;
  margin-bottom: 8px;
}

.gesture-guide ul {
  list-style: none;
  padding-left: 0;
}

.gesture-guide li {
  margin-bottom: 6px;
}


/* Layout */
.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
    position: relative;
    z-index: 1;
}

/* Header Styles */
.header {
    text-align: center;
    margin-bottom: 30px;
    position: relative;
}

.title {
    font-family: 'Cinzel', serif;
    font-size: 3.5rem;
    font-weight: 600;
    color: var(--primary-gold);
    text-shadow: 3px 3px 6px rgba(0,0,0,0.7);
    margin-bottom: 10px;
    letter-spacing: 2px;
}

.subtitle {
    font-size: 1.2rem;
    color: var(--text-dim);
    font-style: italic;
    margin-bottom: 20px;
}

/* Controls */
.controls {
    display: flex;
    justify-content: center;
    gap: 15px;
    margin-bottom: 20px;
    flex-wrap: wrap;
}

.btn {
    background: linear-gradient(145deg, var(--card-bg), rgba(60, 60, 60, 0.8));
    border: 2px solid var(--primary-gold);
    color: var(--text-light);
    padding: 12px 24px;
    border-radius: 8px;
    cursor: pointer;
    font-family: 'Cinzel', serif;
    font-size: 0.9rem;
    font-weight: 600;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.btn:hover {
    background: linear-gradient(145deg, var(--primary-gold), #9a9d3a);
    color: var(--dark-bg);
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(182, 185, 67, 0.3);
}

.btn:active {
    transform: translateY(0);
}

/* API Status */
.api-status {
    text-align: center;
    padding: 10px;
    border-radius: 8px;
    margin-bottom: 20px;
    font-weight: 600;
    transition: all 0.3s ease;
}

.api-status.connected {
    background: rgba(0, 128, 0, 0.2);
    border: 2px solid #00aa00;
    color: #00ff00;
}

.api-status.disconnected {
    background: rgba(139, 0, 0, 0.2);
    border: 2px solid var(--accent-red);
    color: #ff6b6b;
}

/* Story Container */
.story-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 30px;
    margin-bottom: 30px;
}

.story-panel {
    background: var(--card-bg);
    border: 2px solid var(--primary-gold);
    border-radius: 15px;
    padding: 30px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.5);
    backdrop-filter: blur(10px);
    position: relative;
    overflow: hidden;
}

.story-panel::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, var(--primary-gold), var(--accent-red), var(--accent-blue), var(--primary-gold));
    animation: shimmer 3s linear infinite;
    opacity: 0.7;
}

.story-text {
    font-size: 1.2rem;
    line-height: 1.8;
    color: var(--text-light);
    margin-bottom: 20px;
    min-height: 200px;
}

.story-image {
    width: 100%;
    max-height: 400px;
    object-fit: cover;
    border-radius: 10px;
    border: 2px solid var(--primary-gold);
    transition: all 0.3s ease;
}

.story-image:hover {
    transform: scale(1.02);
    box-shadow: 0 15px 40px rgba(182, 185, 67, 0.3);
}

/* Options Frame */
.options-frame {
    background: var(--card-bg);
    border: 2px solid var(--primary-gold);
    border-radius: 15px;
    padding: 25px;
    margin-bottom: 20px;
    backdrop-filter: blur(10px);
}

.options-title {
    font-family: 'Cinzel', serif;
    font-size: 1.4rem;
    color: var(--primary-gold);
    margin-bottom: 20px;
    text-align: center;
    font-weight: 600;
}

.option-button {
    display: block;
    width: 100%;
    background: linear-gradient(145deg, rgba(60, 60, 60, 0.8), rgba(40, 40, 40, 0.9));
    border: 2px solid var(--text-dim);
    color: var(--text-light);
    padding: 15px 20px;
    margin-bottom: 12px;
    border-radius: 10px;
    cursor: pointer;
    font-size: 1.1rem;
    text-align: left;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
}

.option-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(182, 185, 67, 0.1), transparent);
    transition: left 0.5s ease;
}

.option-button:hover {
    border-color: var(--primary-gold);
    background: linear-gradient(145deg, rgba(80, 80, 80, 0.9), rgba(60, 60, 60, 0.9));
    transform: translateX(5px);
    box-shadow: -5px 5px 15px rgba(0,0,0,0.2);
}

.option-button:hover::before {
    left: 100%;
}

/* Gesture Indicator */
.gesture-indicator {
    background: var(--card-bg);
    border: 2px solid var(--accent-blue);
    border-radius: 15px;
    padding: 20px;
    margin-bottom: 20px;
    text-align: center;
    backdrop-filter: blur(10px);
    position: relative;
}

.gesture-indicator.instructions {
    border-color: var(--primary-gold);
    background: rgba(182, 185, 67, 0.1);
}

.gesture-confirmation {
    animation: pulse 1s ease-in-out infinite;
}

/* Settings Frame */
.settings-frame {
    background: var(--card-bg);
    border: 2px solid var(--primary-gold);
    border-radius: 15px;
    padding: 25px;
    margin-bottom: 20px;
    display: none;
    backdrop-filter: blur(10px);
}

.settings-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 15px;
    padding: 10px 0;
    border-bottom: 1px solid rgba(182, 185, 67, 0.3);
}

.settings-row:last-child {
    border-bottom: none;
    margin-bottom: 0;
}

.settings-label {
    font-family: 'Cinzel', serif;
    font-weight: 600;
    color: var(--primary-gold);
}

.slider {
    width: 150px;
    height: 6px;
    border-radius: 3px;
    background: var(--text-dim);
    outline: none;
    opacity: 0.7;
    transition: opacity 0.2s;
}

.slider:hover {
    opacity: 1;
}

.slider::-webkit-slider-thumb {
    appearance: none;
    width: 20px;
    height: 20px;
    border-radius: 50%;
    background: var(--primary-gold);
    cursor: pointer;
}

.dropdown {
    background: var(--card-bg);
    border: 2px solid var(--primary-gold);
    color: var(--text-light);
    padding: 8px 12px;
    border-radius: 5px;
    font-family: 'Cinzel', serif;
}

/* Animations */
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}

@keyframes shimmer {
    0% { transform: translateX(-100%); }
    100% { transform: translateX(100%); }
}

@keyframes pulse {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.05); opacity: 0.8; }
}

@keyframes backgroundShift {
    0%, 100% { transform: scale(1) rotate(0deg); }
    50% { transform: scale(1.1) rotate(1deg); }
}

.fade-in {
    animation: fadeIn 0.8s ease-in;
}

/* Responsive Design */
@media (max-width: 768px) {
    .title {
        font-size: 2.5rem;
    }
    
    .story-container {
        grid-template-columns: 1fr;
        gap: 20px;
    }
    
    .controls {
        flex-direction: column;
        align-items: center;
    }
    
    .story-panel {
        padding: 20px;
    }
}

.witcher-status-box {
  position: fixed;
  top: 20px;
  right: 20px;
  width: 200px;
  background: #1a1a1a url('/static/parchment_texture.jpg') repeat;
  border: 2px solid #b6b943;
  border-radius: 12px;
  color: #e4dcac;
  font-family: 'Mason Serif', Georgia, serif;
  padding: 10px 14px;
  box-shadow: 0 0 8px #b6b943aa;
  display: flex;
  align-items: center;
  gap: 10px;
}

.witcher-sigil {
  width: 36px;
  height: 36px;
}

.status-title {
  font-weight: bold;
  margin-bottom: 4px;
  color: #d6c589;
  font-size: 14px;
}

#apiStatus {
  font-size: 13px;
  color: #e0e0e0;
}
